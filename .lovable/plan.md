# Add Homepage Infrastructure Introduction

## Change
- Replace the current company-introduction block directly below the slideshow with the supplied content.
- Use the supplied hierarchy exactly: one H1, two H2 headings, and three H3 service headings.
- Present the three service offerings in a responsive layout matching the existing spacing, typography, colors, and borders.
- Add the supplied internal service links as standard `<a href>` links.
- Add the final “Talk to an engineer about your infrastructure →” link to `/contact` using the existing primary CTA styling.
- Keep all slideshow headlines as H2 elements and confirm no other H1 remains on the homepage.

## Verification
- Check heading counts and order on the rendered homepage.
- Confirm all internal links use the requested destinations and the new section sits immediately below the slideshow.
- Check desktop and mobile presentation, then confirm the latest preview build is successful.
